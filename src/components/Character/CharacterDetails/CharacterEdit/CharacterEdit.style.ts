import { InputNumber as AntInputNumber, Row } from 'antd';
import styled from 'styled-components';

export const InputNumber = styled(AntInputNumber)`
  width: 100%;
`;

export const DropdownHeader = styled(Row)`
  padding: 8px;
`;
